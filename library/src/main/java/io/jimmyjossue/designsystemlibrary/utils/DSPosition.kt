package io.jimmyjossue.designsystemlibrary.utils

enum class DSHorizontal { LEFT, RIGHT }
enum class DSAlignHorizontal { LEFT, CENTER, RIGHT }
enum class DSVertical { TOP, BOTTOM }
enum class DSPosition { TOP, LEFT, RIGHT, BOTTOM }
